
import { tagButton } from 'dinner-and/helpers/tag-button';
import { module, test } from 'qunit';

module('Unit | Helper | tag button');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tagButton([42]);
  assert.ok(result);
});

