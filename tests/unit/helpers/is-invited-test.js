
import { isInvited } from 'dinner-and/helpers/is-invited';
import { module, test } from 'qunit';

module('Unit | Helper | is invited');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isInvited([42]);
  assert.ok(result);
});

