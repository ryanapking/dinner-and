
import { dateStamp } from 'dinner-and/helpers/date-stamp';
import { module, test } from 'qunit';

module('Unit | Helper | date stamp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateStamp([42]);
  assert.ok(result);
});

