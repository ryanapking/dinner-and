
import { routeReturn } from 'dinner-and/helpers/route-return';
import { module, test } from 'qunit';

module('Unit | Helper | route return');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routeReturn([42]);
  assert.ok(result);
});

